---
const { text, active } = Astro.props;
---

<span class={`inline-flex cursor-pointer items-center rounded-full px-4 py-2 text-xs font-medium hover:scale-105 hover:text-black hover:bg-green-500 transition duration-200
  ${active ? 'bg-green-500 text-black' : 'bg-gray-800'} `}
>
  {text}
</span>
